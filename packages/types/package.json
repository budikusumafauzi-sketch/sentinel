{
  "name": "@sentinel/types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types and contracts for Sentinel",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "clean": "rimraf dist",
    "lint": "echo \"No lint config for types yet\"",
    "test": "echo \"No tests yet\""
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "rimraf": "^6.0.1"
  }
}
